# Bridge

This directory is reserved for `flutter_rust_bridge` v2 generated Dart bindings.

The Rust API surface starts at `rust/src/api/mod.rs`. The current environment had
an older `flutter_rust_bridge_codegen` binary available, so generated bindings are
not checked in from this pass.
