server {
    listen 80;
    server_name _;

    location = /telegram_patterns.yaml {
        root /usr/share/nginx/html;
        default_type text/yaml;
        add_header Cache-Control "no-cache" always;
    }

    location / {
        return 404;
    }
}
